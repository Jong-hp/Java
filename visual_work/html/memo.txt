html(정보), css(디자인), javascript(언어)

java, jsp, spring

oracle database

vue 등등

<> markup

css 

요소명{
        속성명 : 적용할 값;
        }
        
font size - 1em = 16px

xxx="" - attribute 속성

git

init - git을 사용
add - 저장
commet - commet의 대상을 설정
branch - 테스트 할 수 있는 공간 (-d - 삭제)
reset
merge - 병합


git(저장소)

1./git_test/파일명 폴더를 만든다.
2.init을 해서 git local 저장소로 만든다.
3.git add 파일명 저장한다.
4.git remote add origin 원격 저장소의 위치를 등록한다.
5.git push -u origin master 푸쉬로 넣어준다.
